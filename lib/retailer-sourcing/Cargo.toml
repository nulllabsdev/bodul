[package]
name = "retailer-sourcing"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
shared = { path = "../shared" }
money = { path = "../money" }
regex = "1.12.4"
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
